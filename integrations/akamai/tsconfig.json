{
  "extends": "./tsconfig.build.json",
  "compilerOptions": {
    "experimentalDecorators": true,
    "noImplicitAny": false,
    "baseUrl": ".",
    "paths": {
      "@netacea/*": ["../../modules/*/src"],
      "@netacea/akamai/package.json": ["./package.json"],
      "http-request": ["./package.json", "<-- External import. This stops tests complaining about import not found"],
      "create-response": ["./package.json", "<-- External import. This stops tests complaining about import not found"],
      "log": ["./package.json", "<-- External import. This stops tests complaining about import not found"],
    }
  },
  "include": [
    "index.ts",
    "src/**/*.ts",
    "tests/*.ts",
    "types/*.ts"
, "unit_tests/*.ts"  ]
}
